# scene-3: AI 에이전트 연결 (스토리보드 #3, 0:24–0:33)

MCP 호출 없음. 심사위원에게 "이 모든 작업이 Claude 안에서 MCP 플러그인으로 작동한다"를
보여주는 장면 — 연결 칩은 골격의 `.app-bar`에 이미 있으므로, 이 장면은 Andy가
`@Laighthouse`를 입력하는 첫 말풍선만 그린다.

## 대사 (그대로)

- **{persona_name}**: `@Laighthouse`

## HTML

```html
<div class="scene-divider">Scene 3 · AI 에이전트 연결</div>
<div class="msg user">
  <div class="avatar user">{persona_name 첫 글자}</div>
  <div><div class="sender">{persona_name}</div>
    <div class="bubble"><span style="background:rgba(255,255,255,.2); border-radius:6px;
      padding:1px 6px; font-weight:600;">@Laighthouse</span></div></div>
</div>
```
